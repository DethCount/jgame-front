import { ConstructionRequestObserver } from './construction-request-observer';

describe('ConstructionRequestObserver', () => {
  it('should create an instance', () => {
    expect(new ConstructionRequestObserver()).toBeTruthy();
  });
});
